#include <stdio.h>

void copiaLinha(int n, FILE *f, FILE *g)

{
	int a;

	f = fopen(total.txt,"r");

    while(){
    	n = fgetc(total.txt);
        a = fputc(total.txt,par.txt); 
    }

}

void saltaUmaLinha(FILE *f)

{
    int b,c;

    f = fopen(total.txt,"r");

    while(c!=EOF){
    	b = fgetc(total.txt)
    	
    	if(b="\n"){
        
    	}
    }

}



void main()

{
	FILE *f;
	FILE *g;
	int n;

    copiaLinha(n,*f,*g);
    saltaUmaLinha(*f);

}